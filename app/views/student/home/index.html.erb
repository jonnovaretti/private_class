<div class="container">
    <div class="card" style="width: 18rem;">
    <div class="card-body">
        <h5 class="card-title"><%= @student.professor.name %></h5>
        <h6 class="card-subtitle mb-2 text-muted">You are in the level Basic II</h6>
        <p class="card-text">Your course started at <%= @student.created_at.strftime("%d/%m/%y") %></p>
        <a href="#" class="card-link">See lessons</a>
        <a href="#" class="card-link">See tests</a>
    </div>
    </div>
</div>